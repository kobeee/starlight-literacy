---
description: 微信小程序前端开发规范（Skyline 渲染引擎）
globs: src/miniprogram/**
alwaysApply: false
---

# 微信小程序开发规范

## 渲染引擎
使用 Skyline 渲染引擎 + glass-easel 组件框架。

## 配置要求
```json
{
  "renderer": "skyline",
  "componentFramework": "glass-easel",
  "lazyCodeLoading": "requiredComponents"
}
```

## 交互设计红线（PRD 强制）
- 一屏只做一件事，信息密度极低
- 所有可点击元素最小触控区域 48×48pt
- 每个操作即时视听反馈（≤100ms 响应）
- 不出现任何文字菜单/导航——全靠图形和颜色引导
- 错误反馈温和，绝不使用"错了""失败"等负面文案

## 视觉规范
- 学习界面底色：`#FFF8F0`（暖白护眼）
- 星空主题：深蓝渐变底色、星星元素、微光粒子
- 字体：圆润儿童友好体，大号呈现（汉字 ≥72pt）
- 动效：弹性缓动为主，轻快不花哨

## 动画开发
- 优先使用 Skyline Worklet 动画（渲染线程同步执行）
- 手势交互使用 Skyline 手势系统
- 页面转场使用共享元素动画或自定义路由
- 目标帧率 60fps

## 代码分层
```
pages/        ← UI 层：页面视图和交互，平台专属
components/   ← UI 层：可复用组件，平台专属
services/     ← 封装层：所有后端调用通过此层，不在页面中直接写 wx.cloud
core/         ← 逻辑层：出题算法、评分、进度计算，纯 JS，不依赖 wx API
utils/        ← 工具层：通用辅助函数
```
- `services/` 是后端调用的唯一出口，页面和组件通过 service 获取数据
- `core/` 中禁止 import 任何 `wx.*` API，保持纯函数风格

## 目标用户
3~6 岁幼儿 + 家长。代码中所有面向孩子的文案要简短、积极、温暖。
