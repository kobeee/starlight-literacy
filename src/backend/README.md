# Backend

Backend code lives here.

Current Mobile H5 MVP does not require a live backend. It keeps learning progress and payment intent in `localStorage` so the market-validation build can run as a static app.

The existing WeChat cloud placeholder is kept in `src/backend/functions/` for a future miniprogram backend or a payment entitlement service.
